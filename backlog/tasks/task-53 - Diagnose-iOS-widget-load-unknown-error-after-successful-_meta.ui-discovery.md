---
id: TASK-53
title: Diagnose iOS widget-load "unknown error" after successful _meta.ui discovery
status: In Progress
assignee: []
created_date: '2026-08-16 23:37'
updated_date: '2026-08-17 14:40'
labels: []
dependencies: []
priority: high
type: bug
ordinal: 59000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
After v0.5.1 (gating removed), a force-quit cold start + new conversation on Claude iOS ("can you show me my daily nutrition stats") produced: text answer correct (stateless tools/call get_goal_progress → 200 via backend relay) AND "Failed to load the MCP app, an unknown error has occured". Progress vs. prior failures: the app now ATTEMPTS the widget (proves it saw _meta.ui in tools/list). But no resources/ui read appears in the captured nom-mcp log window (23:29:51-52, ~1s, ends at the tool call). Two hypotheses: (a) log window truncated before the widget load (host fetches ui:// resource seconds later); (b) the widget loader is a separate app-initiated connection that fails at the auth proxy (WKWebView without Authelia cookie → 401) and never reaches nom-mcp. Distinguish via: full nom-mcp journal from app-open through >1min after the error (look for resources/read:ui://nom-mcp/goal-progress + status), and auth-proxy logs for the same window (look for 401/403). If (a) and resources/read=200: failure is inside the WebView (ui/initialize handshake / CSP / sandbox) — next layer is host-side. If (b): fix is proxy auth passthrough for the widget-loader connection.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-08-16 diagnosis COMPLETE (full proxy log + journalctl windows 23:29:10–23:31:00): (1) App launch 23:29:14–15: stateless server/discover + resources/list + tools/list all 200, ui_meta_tool_count=2 — app received widget metadata. (2) Prompt: stateless tools/call:get_goal_progress 200 at 23:29:52. (3) ONLY subsequent request: session-based ping on 5a42d5e2 (2025-11-25) at 23:30:35 → 200 — iOS session healthy after the failure. (4) ZERO resources/read requests from any channel/stateless or session. (5) Proxy: all requests 200, no auth failures — proxy exonerated. MYSTERY SOLVED: UA 'python-httpx/0.28.1' in proxy logs IS the iOS app's session-based MCP client (same session id + protocol as 22:24 log); the 23:30:35 request was a routine ping. CONCLUSION: Claude iOS 1.260813.0 fails locally between receiving the tool result and issuing resources/read — client-side renderer bug/limitation. All server/proxy layers verified healthy; web renders widgets off identical responses. No further server-side fix possible. Options: report to Anthropic; optionally re-gate _meta.ui for claude-ios so iOS stops attempting (text-only fallback, as pre-v0.5.1) until Anthropic ships a fixed renderer.

2026-08-17 00:15-00:31 UPDATE — regression went cross-surface: after iOS 'Failed to Load' (~00:19) web ALSO stopped rendering widgets ('Unable to reach Nom' banner, then text-only answers). Log evidence: every request reaching nom-mcp AND the proxy returned 200 (zero panics, server up since 22:06); yet client traffic collapsed — 5 user re-runs produced ~no MCP traffic; even a BRAND-NEW web conversation (00:28:34 discover + tools/call:get_weekly_progress) carried NO tools/list and NO resources/read — the Anthropic backend relay drives all widget behavior from its own cached catalog/UI state and never re-polls us (our 5-min ttlMs hints not honored on the stateless inline-lifecycle channel). Web had previously rendered widgets WITHOUT ever sending us a resources/read (served HTML from its own cache). Suppression confirmed per-SERVER (new conversation also text-only). Conclusion: Anthropic-side per-server degraded state / circuit breaker, plausibly triggered by the reported iOS load failure; nothing we deploy can reach that state. Mitigation being tested: toggle the MCP server OFF->ON in Claude settings to force full re-handshake + fresh catalog/resource prefetch. Also implemented (pending release): ui:// resources now listed in resources/list (spec permits omission; some hosts apparently cross-check tool resourceUri against the listing before fetching). NOTE: python-httpx/0.28.1 UA in proxy logs = iOS app's session-based MCP client (routine pings); Claude-User UA = stateless relay channel.

2026-08-17 01:29 DEPLOYED v0.5.2 (tag fb6e51a): ui:// resources now listed in resources/list. CD pipeline ~15min total (build+publish+notify, then homelab-side deploy workflow ~5min). Live at 01:29:13 UTC (new store path lfvs6qmn...); direct production resources/list probe confirms 3 entries incl. both ui:// URIs with text/html;profile=mcp-app. Service restart transparent to existing sessions (McpSessionStore restore working — iOS session pings continued across restart). NEXT: user runs OFF->ON toggle test in Claude settings (web) against v0.5.2 to force fresh relay handshake; watch journal for tools/list burst (ui_meta_tool_count=2) + any resources/read, and whether widgets render.

2026-08-17 01:34-01:40 RESOLVED (web) — toggle test PASSED: user toggled nom-mcp OFF->ON at ~01:34 UTC; journal shows the full fresh-handshake chain, all 200 OK: stateless server/discover x6 + tools/list x3 (ALL ui_meta_tool_count=2) + resources/list x2 (now 3 entries incl. ui:// URIs) -> tools/call:get_weekly_progress -> resources/read:ui://nom-mcp/weekly-progress x3 (once per page refresh). FIRST PRODUCTION resources/read FOR A WIDGET EVER PREVIOUSLY THE RELAY RENDERED FROM ITS OWN CACHE WITHOUT FETCHING. Widget rendered on first request AND survived repeated page refreshes. CONFIRMED: the cross-surface text-only regression was Anthropic-side per-server degraded state; the OFF->ON toggle forces a full re-handshake that clears it. Mitigation documented: if widgets stop rendering with all server responses healthy (200s, no panics, collapsed client traffic), toggle the MCP server off/on in Claude settings. REMAINING: iOS 'Failed to Load' (renderer fails before issuing resources/read) is untested against v0.5.2 — the new resources/list content may or may not matter to it; next step is an iOS cold-start test, then decide report-to-Anthropic vs re-gate claude-ios.

2026-08-17 01:41 iOS RE-TEST (v0.5.2) FOUND ROOT CAUSE #3 — protocol-version negotiation abort. Journal+proxy window 01:40-01:45: stateless server/discover + tools/call:get_weekly_progress both 200 (text answer rendered), THEN at 01:41:22.819 a NEW SESSION-BASED initialize arrived (UA Claude-User = relay, session a2669b36) requesting protocolVersion '2026-01-26' (the MCP Apps spec revision) with capabilities.extensions['io.modelcontextprotocol/ui'] = {mimeTypes: [text/html;profile=mcp-app]}. rmcp 3.1.2 (latest on crates.io) does not know that revision: WARN 'client requested unsupported protocol version; falling back to server default client_requested=2026-01-26 server_fallback=2025-11-25'. Local repro confirmed our initialize RESPONSE echoes protocolVersion 2025-11-25. Relay accepted the 200+session-id but NEVER sent anything else on that session (zero follow-ups in nom-mcp AND proxy logs; all 6 proxy reqs 200) -> app shows 'Failed to load the MCP app. Unable to connect to server' (note: different message than the earlier 'unknown error'). CONCLUSION: relay requires its requested revision echoed back; the downgrade reads as failed handshake. FIX (v0.5.3, commit 4179a7f): override ServerHandler::supported_protocol_versions() in McpHandler to include 2026-01-26 alongside rmcp's KNOWN_VERSIONS (negotiation echoes any listed version). Verified locally: initialize 2026-01-26 -> echo 2026-01-26; full session flow through resources/read works under that negotiated version; 2025-11-25 still echoes normally. DEPLOYED 02:24 UTC; production probe confirms echo of 2026-01-26 with serverInfo 0.5.3. NEXT: user re-runs iOS cold-start test against v0.5.3; expect initialize(2026-01-26) FOLLOWED BY resources/read:ui://... on the same session, and a rendered widget.

2026-08-17 02:26 iOS RE-TEST vs v0.5.3 (echo fix) — SERVER SIDE NOW PROVABLY FULLY HEALTHY; FAILURE MOVED UPSTREAM. Journal 02:26:59 (all within 1.4ms, all 200 OK, jsonrpc_errors=None): relay REUSED the session id a2669b36 from the failed 01:41 attempt (cached client-side); our McpSessionStore transparently restored it across the 02:24 deploy restart (v0.4.6 feature doing its job); rmcp replayed stored initialize (protocol 2026-01-26, clientInfo claude-ios 1.260813.0) -> notifications/initialized -> resources/read:ui://nom-mcp/goal-progress ok=true -> 200. negotiated_protocol_version=2026-01-26 confirmed (no more downgrade WARN). Proxy window 02:25-02:32: 7 reqs ALL 200, all POST /mcp, NO app-initiated connections (WKWebView-without-cookie hypothesis re-exonerated), NO GET/SSE streams — identical to the WEB success channel which also never uses GET. Local byte-level replication of the exact sequence (create session w/ 2026-01-26 + claude-ios identity -> kill+restart server -> resources/read on stale session id) returns 200 with correct SSE framing + full HTML. Widget HTML audited: self-contained (no external URLs, no script src, no CSP meta). CONCLUSION: every layer we control works end-to-end for iOS; the 'Failed to load the MCP app. Unable to connect to server' failure occurs after the relay receives our 200+HTML — i.e., in the relay->app handoff or the app's WKWebView container/bootstrap (host-side). No further server-side fix available. DECISION PENDING: (A) report to Anthropic w/ full evidence chain (initialize params, successful read, absent follow-up traffic, web-vs-iOS contrast); (B) re-gate _meta.ui for claude-ios clients (text-only fallback, no error banner) until Anthropic ships a fixed renderer; (C) both (recommended); (D) leave as-is.

2026-08-17 UPSTREAM RESEARCH (user-sourced GitHub threads: anthropics/claude-ai-mcp#40 comments 5116591786+4288038667, #165 comment 5142200204, modelcontextprotocol/ext-apps repo). FINDINGS: (1) hadarge (#40, Jul 29): Claude iOS v1.260716 sandbox loads widgets at origin {hash}.claudemcpcontent.com with query platform=ios&resource-src=https://assets.claude.ai and enforces a HARDCODED CSP ignoring declared _meta.ui.csp (default-ish: script/style allow 'unsafe-inline'; img 'self' data: blob: assets.claude.ai; connect 'self'). Our widget is fully self-contained (inline style/script, zero external URLs, postMessage targetOrigin '*') -> UNAFFECTED by that bug. Also: tool results >~150k chars get offloaded to sandbox FS and never hydrated (ours ~10KB, fine). (2) ochafik = ANTHROPIC STAFF (#40, Apr 21): host previously read _meta only from resources/read RESULT; now also defaults to the resource's own _meta (ext-apps PR #410). (3) Booyaka A/B probe (#165, Jul 31, 36 runs vs claude.ai web): _meta.ui.domain ABSENT -> mounts fine (sandbox origin stable PER CONVERSATION); PRESENT BUT WRONG -> 0/8 renders, explicit 'Unable to reach' error (fatal, incl. web); correct sha256(endpoint)[:32]+'.claudemcpcontent.com' -> pinned stable origin. ALSO: intermittent host-side blank-render (6/10 mounted-but-never-painted despite successful resources/read, later 0/18) with WORKAROUND = disconnect/reconnect the connector (per-connector session state, matches OUR off->on toggle finding); stale ui:// URIs produce the SAME 'Unable to reach' error (we keep URIs stable across versions - not our case). SPEC (ext-apps specification/2026-01-26/apps.mdx): _meta.ui.{csp,domain,permissions,prefersBorder} belong in the resources/read contents[] entries (docs/csp-cors.md line 61 confirms); restrictive default CSP when csp omitted = script-src/style-src 'self' 'unsafe-inline' etc -> our self-contained HTML passes it. NEXT STEPS: (A) USER TEST NOW: remove + re-add the nom-mcp connector in Claude settings (flushes relay/app per-connector state incl. the poisoned a2669b36 session minted under broken v0.5.2) then iOS cold-start re-test; monitor journal. (B) IF STILL BROKEN: add _meta.ui.domain = sha256(exact connector URL)[:32]+'.claudemcpcontent.com' to tools/list _meta.ui AND resources/read contents _meta (NEEDS exact registered URL string from user - scheme/host/path/trailing-slash exactly as entered; wrong value is FATAL for web too, so verify web still renders before iOS test). (C) Report to Anthropic referencing #40/#165/ext-apps#671 remains open option.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-08-17 04:20
---
## 2026-08-17 — reconnect evidence, todoist diff, hash hunt, `_meta.ui.domain` shipped (v0.5.4)

**Reconnect window (03:05–03:15 UTC).** User disconnected/reconnected the connector (mobile offers no full removal). Journal shows: fresh stateless discovery bursts (`server/discover`+`tools/list`+`resources/list`, header protocol `2026-07-28`) at 03:05:24 and 03:10:51, then FOUR fresh `initialize` sessions (03:05:42, 03:05:53, 03:06:10, 03:11:35) — two followed by `notifications/initialized` carrying `MCP-Protocol-Version: 2026-01-26` (our v0.5.3 echo fix confirmed working in production). STILL ZERO `resources/read` calls — the iOS-side loader dies between init and fetch, same as before. Failure remains upstream of our fetch path.

**Todoist reference diff** (Doist/todoist-mcp @311ed1f, known-working): they emit identical `_meta.ui` on BOTH the resource registration and the read-result contents entry (we now mirror the read side); they set explicit `csp: {connectDomains:[], resourceDomains:[]}` + `prefersBorder: true` (we omit — spec default CSP suffices for our self-contained widgets); they do NOT set `domain`; their URIs are content-versioned (`ui://todoist/task-list@{sha256(html)[:12]}`) — noted as future cache-busting hardening, deliberately NOT adopted this cycle (single-variable experiment).

**Hash hunt.** User observed the actual web sandbox origin: `ccedd2f0677de1b05856b55902232949.claudemcpcontent.com`. That does NOT equal sha256[:32] of `https://nom-mcp.home.jeffutter.com/mcp` (= `403303330a52bde603a808b90b7dc135`). Brute-forced ~31 URL variants (scheme/host/port/path incl. trailing slash) × md5/sha1/sha256 first/last-32: no match. Derivation is opaque (per-conversation mint, or an internal canonical form we can't see) — which is exactly why the value is now config, not baked in.

**Implementation (v0.5.4).** Optional `AppConfig::ui_domain` (TOML `ui_domain` / env `NOM_MCP_UI_DOMAIN`); `McpHandler::with_ui_domain(Option<String>)` builder wired at both serve call sites; `_meta.ui.domain` emitted on gated `tools/list` AND on `ui://` read-result contents entries (rmcp `TextResourceContents.meta` via `with_meta`); `resourceUri` always present, `domain` only when configured (a subtly wrong value is reported fatal even on web, so never emit unless explicitly set). 5 new tests; gates green (fmt, clippy -D warnings, 308/308 nextest, doctests). Local e2e with `NOM_MCP_UI_DOMAIN` set: both surfaces emit `{"domain":"ccedd2f0...","resourceUri":"ui://nom-mcp/goal-progress"}`; unset → key absent.

**Deploy note for user.** Add to the secrets-render source behind `/run/secrets/rendered/nom-mcp.env` (file is re-rendered each deploy — manual edits don't survive):
`NOM_MCP_UI_DOMAIN=ccedd2f0677de1b05856b55902232949.claudemcpcontent.com` (primary: the origin Claude itself minted/served for us on web). Fallbacks, no release needed — one-line change + service restart: computed `403303330a52bde603a808b90b7dc135.claudemcpcontent.com`, or remove the line entirely (field omitted, host default origin).
---

created: 2026-08-17 14:40
---
## 2026-08-17 ~11:30 UTC — domain experiment CONCLUDED (inert for iOS); pivoting to upstream report

**v0.5.4 live** (store path ryy0hhz3..., ExecStart 0.5.4). User chose the COMPUTED hash `403303330a52bde603a808b90b7dc135.claudemcpcontent.com` for NOM_MCP_UI_DOMAIN; production probe confirms both gated tools/list AND resources/read contents emit `{domain, resourceUri}`.

**Web re-test (~11:09 UTC): PASSES with domain set.** Stateless Claude-User burst: server/discover → resources/list → tools/list → resources/read:ui://nom-mcp/goal-progress, all 200 (client_name=claude-ai, header protocol 2026-07-28). Widget rendered.

**iOS re-test (10:29:13 UTC): SAME FAILURE, now with domain pinned.** Fresh session 9c8ef666: initialize @2026-01-26 + io.modelcontextprotocol/ui extension → 200 + echoed version + Mcp-Session-Id; notifications/initialized at 10:29:25 → 202; then ZERO requests from that session ever. Banner: 'Failed to load the MCP app. Unable to connect to server' (same variant as v0.5.3 era). Proxy: everything 200. Verdict: `_meta.ui.domain` pinning is INERT for iOS — the loader dies between init and fetch regardless. Server-side levers exhausted (session persistence ✓, gating removed ✓, protocol echo ✓, resources/list ui:// entries ✓, domain pin ✗).

**Re-gate claude-ios ruled out INFEASIBLE:** the stateless discovery channel (where the app learns about widgets) is anonymous — clientInfo.name is only known inside the session-based widget-loader initialize, i.e. after the attempt has already started. Hiding _meta.ui there can't prevent the attempt; hiding it on stateless would break web too. This is also why the original 81d32ff gate never fully worked.

**Decisions:** (1) USER ACTION PENDING: unset NOM_MCP_UI_DOMAIN (remove line from render source + service restart) — revert to field-omitted (spec default; Todoist parity; removes opaque variable). (2) Upstream report drafted: `docs/ios-widget-upstream-report.md` — full evidence chain (Case A fresh-session silence, Case B successful-read-still-fails, web control), five ruled-out layers, and three concrete asks incl. relay logs for session 9c8ef666-24cd-4994-a772-3a27cda5866f. File against anthropics/claude-ai-mcp#40 (ochafik active there) or a fresh issue. (3) Optional future datapoint: user tries a known-working third-party widget (Todoist) on their Claude iOS to establish app-wide vs our-server scope. **TASK-51 (debug middleware removal) stays OPEN** — the tracing lines remain valuable while the Anthropic thread is active.
---
<!-- COMMENTS:END -->
